Разобраться и научиться отличать, где вина Backend и Frontend.

Последовательность действий в git:
{
git add .
git commit -m ""

git fetch && git merge origin/master - подтягиваем себе актуальную версию мастера:

merge и commit похожи.

- Смотрим конфликты
git push
}

Docker лучше поднимать на Linux-е - увеличивается скорость работы с контейнерами,
а именно их запуск, т.к. если не используем линукс - поднимается сначала ядро линукс,
а потом ядро приложения, т.к. Docker имеет ядро Linux.
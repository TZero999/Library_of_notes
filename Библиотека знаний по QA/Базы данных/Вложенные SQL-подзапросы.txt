Если мы не можем с помощью одного SELECT-а и указанных в нём условий, вывести значение,
которое нас интересует - можно использовать еще один SELECT внутри нашего запроса.

- SELECT fname,lname,gender,id FROM general WHERE age IN (SELECT age FROM general WHERE id>1 and age >=21);
Вывести полное имя, пол и ID-номер студента, восзраст которого подходит под второе условие (id>1, возраст >= 21 год)